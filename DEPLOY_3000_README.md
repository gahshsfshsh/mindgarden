# 🚀 Деплой на сервер 188.68.223.230:3000

Итоговые скрипты для безопасного деплоя MindGarden на порт 3000 без поломки других процессов.

## 📋 Варианты использования

### Вариант 1: Запуск через SSH одной командой (РЕКОМЕНДУЕТСЯ)

Скопируйте и выполните команду из файла `DEPLOY_COMMAND.txt`:

```bash
ssh root@188.68.223.230 'GITHUB_TOKEN="YOUR_GITHUB_TOKEN_HERE"; REPO_DIR="$HOME/mindgarden"; BACKEND_DIR="$REPO_DIR/backend"; PORT=3000; PID=$(lsof -ti:$PORT 2>/dev/null); [ ! -z "$PID" ] && kill -9 $PID 2>/dev/null && sleep 2; pkill -f "uvicorn.*$PORT" 2>/dev/null || true; sleep 1; if [ -d "$REPO_DIR" ]; then cd "$REPO_DIR" && git pull https://${GITHUB_TOKEN}@github.com/gahshsfshsh/mindgarden.git 2>/dev/null || git pull origin main 2>/dev/null || true; else cd "$HOME" && git clone https://${GITHUB_TOKEN}@github.com/gahshsfshsh/mindgarden.git; fi; cd "$BACKEND_DIR" || exit 1; [ ! -d "venv" ] && python3 -m venv venv; source venv/bin/activate; pip install -q -r requirements.txt; [ ! -f ".env" ] && cat > .env << '\''EOF'\''
DATABASE_URL=sqlite:///./mindgarden.db
JWT_SECRET=mindgarden-dev-secret-key-2024
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
EOF
[ -f "seed_content.py" ] && python3 seed_content.py 2>/dev/null || true; nohup python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT > server.log 2>&1 & sleep 4; echo "✅ Сервер запущен на порту $PORT"; echo "📋 PID: $(lsof -ti:$PORT)"; echo "📝 Логи: tail -f $BACKEND_DIR/server.log"; curl -s http://localhost:$PORT/health || echo "⚠️  Проверьте логи"'
```

### Вариант 2: Скопировать скрипт на сервер

```bash
# На вашем компьютере
scp deploy_3000_one_line.sh root@188.68.223.230:~/

# На сервере
ssh root@188.68.223.230
chmod +x ~/deploy_3000_one_line.sh
~/deploy_3000_one_line.sh
```

### Вариант 3: Полный скрипт с проверками

```bash
# На вашем компьютере
scp deploy_server_3000.sh root@188.68.223.230:~/

# На сервере
ssh root@188.68.223.230
chmod +x ~/deploy_server_3000.sh
~/deploy_server_3000.sh
```

## 🔒 Безопасность

Скрипты **безопасно** останавливают только процессы на порту 3000:
- ✅ Проверяют, что процесс на порту 3000 - это Python/uvicorn
- ✅ Останавливают только процесс на порту 3000
- ✅ Не трогают другие порты и процессы
- ✅ Используют GitHub токен для доступа к репозиторию

## 📊 Что делает скрипт

1. ✅ Останавливает только процесс на порту 3000 (безопасно)
2. ✅ Клонирует/обновляет репозиторий с GitHub токеном
3. ✅ Настраивает виртуальное окружение Python
4. ✅ Устанавливает зависимости
5. ✅ Создает .env файл (если не существует)
6. ✅ Инициализирует базу данных
7. ✅ Запускает сервер на порту 3000
8. ✅ Проверяет health endpoint

## 🔧 Управление после деплоя

### Посмотреть логи
```bash
ssh root@188.68.223.230 "tail -f ~/mindgarden/backend/server.log"
```

### Остановить сервер
```bash
ssh root@188.68.223.230 "pkill -f 'uvicorn.*3000'"
```

### Проверить статус
```bash
ssh root@188.68.223.230 "curl http://localhost:3000/health"
```

### Перезапустить
Просто выполните команду деплоя снова - она безопасно перезапустит сервер.

## ⚠️ Важно

1. **GitHub токен** уже встроен в скрипты: `YOUR_GITHUB_TOKEN_HERE`
2. **OPENAI_API_KEY** нужно заполнить в `.env` файле на сервере
3. Скрипт не ломает другие процессы - останавливает только порт 3000
4. Репозиторий: `gahshsfshsh/mindgarden`
5. Сервер: `188.68.223.230:3000`

## 📝 Структура файлов

- `deploy_server_3000.sh` - Полный скрипт с проверками и цветным выводом
- `deploy_3000_one_line.sh` - Упрощенная версия для быстрого запуска
- `DEPLOY_COMMAND.txt` - Готовая команда для копирования
- `start_server_3000.sh` - Скрипт только для запуска (без деплоя)
- `start_3000_simple.sh` - Простой скрипт запуска


